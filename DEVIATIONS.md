# Deviations from Codex 0.136.0

The single, reviewed registry of every place codexgo intentionally diverges from the
behavior or output of the reference Codex build. The parity goal is *zero unexplained*
diffs — every entry below has a rationale. **Material** deviations (user-visible
behavior or ecosystem interop) are marked `review` and need maintainer sign-off
(see `docs/ROADMAP.md` §8); non-material ones are `accepted`.

Categories: `format` (on-disk), `protocol` (wire), `behavioral`, `cosmetic`.

| Spec | Category | Deviation | Rationale | Status |
|------|----------|-----------|-----------|--------|
| 01 abspath | behavioral | `dunce` crate → stdlib `filepath` + hand-rolled verbatim/UNC prefix stripping | Go already emits friendly Windows paths; verbatim stripping replicated in `normalizeWindowsDevicePath` | accepted |
| 01 abspath | behavioral | serde thread-local base-path guard → explicit base param (`Unmarshal(data, base)` / `Decoder`) | Go has no thread-locals; explicit base is idiomatic and equivalent | accepted |
| 01 abspath/pathutil | behavioral | `Path::canonicalize` → `filepath.EvalSymlinks`+`Abs` | closest stdlib analogue; macOS `/var`→`/private/var` may differ on some paths | accepted |
| 01 fuzzymatch / 20 filesearch | behavioral | `nucleo` ranking → custom `fuzzymatch` scorer | exact nucleo scores are impractical to reproduce; result **set** preserved, **order** may differ slightly | review |
| 20 filesearch | behavioral | `ignore` crate → hand-rolled `.gitignore` matcher | supports comments/negation/anchoring/`**`/`[...]`; not bit-exact to git for exotic escapes | review |
| 20 filesearch | behavioral | async streaming search session omitted; only synchronous `Run` ported | `Run` is the value-producing entry point; streaming session can be added later | accepted |
| 20 filewatcher | behavioral | `notify` recursive watch → fsnotify (non-recursive OS watch; recursive flag is semantic-only) | fsnotify has no recursive `Add`; synthetic events + direct children match codex; deep recursive OS delivery is platform-dependent | review |
| 20 filewatcher | behavioral | RAII `Drop` → explicit idempotent `Close()`; tokio `Notify` → `sync.Cond` | Go has no destructors/async runtime; semantics preserved | accepted |
| cross-cutting | behavioral | tokio async (channels, `Arc<Mutex>`, `CancellationToken`) → goroutines/channels/`sync`/`context` | language-idiomatic concurrency; observable ordering preserved | accepted |
| cross-cutting | format | JSON object key ordering (`encoding/json` sorts map keys) | parity asserted via canonicalizer (`internal/paritytest`); struct field order is controlled explicitly where it matters | accepted |
| 02 protocol | format | internally/externally-tagged serde enums → single struct + `Kind` discriminator + custom `MarshalJSON`/`UnmarshalJSON` | Go lacks sum types; wire JSON is preserved exactly | accepted |
| 02 protocol | cosmetic | `op.go` is 1193 lines (> 800 guideline) | `Op` marshal/unmarshal form one cohesive 25-variant unit; splitting hurts cohesion | accepted |
| 03 execpolicy | behavioral | `network_rule`, `amend.rs`, and the clap CLI omitted (scoped to the prefix-rule matcher) | belong to network-policy (spec 15) and CLI (spec 41); the matcher itself is faithful | accepted |
| 03 execpolicy | behavioral | Starlark f-strings not enabled (`go.starlark.net` has none) | codex's bundled policies use no f-strings | accepted |
| 12 sandbox | behavioral | macOS seatbelt complete (.sbpl generation + `Backend` + `sandbox-exec` spawn + policy model/matrix/resolution, with golden + darwin behavioral tests). Linux/Windows backends return not-implemented | Linux/Windows are separate XL specs 13/14; macOS is the dev/parity target for the `codex exec` milestone | accepted |
| 25 code-mode | behavioral | Complete: CodeModeService/Session engine + exec/wait on goja + cell isolation + ToolInvoker bridge. goja is ES5.1+/partial-ES2015+ vs V8 | goja is cgo-free (project goal); JS-feature gaps documented; feature-flagged off by default | accepted |
| 41/42 CLI | behavioral | Default (no subcommand) prints "TUI not yet implemented" + exit 1 (until TUI lands); archive/unarchive, login --device-auth, mcp add/remove, debug prompt-input, non-stdio app-server transports give a clear notice + non-zero exit; exec/mcp-server run against the mock model until a real provider client is wired | turn-driving + diagnostic surface is complete; the rest are clear notices (never silent) and land with TUI / real-model wiring | review |
| 41 completion | behavioral | **Closed (byte-identical).** `codex completion {bash,elvish,fish,powershell,zsh}` is now byte-for-byte identical to the reference binary, asserted by the env-gated `TestParityCompletion` (all 5 shells + the default-is-bash case). bash is a faithful port of clap_complete v4.5.65's `bash.rs` generator (header / for-loop dispatch / `subcommand_details` / `option_details_for_path` templates) driven by an embedded command-tree model (`internal/cli/completion_tree.go`, extracted byte-exact from codex's output: 235 nodes, per-node opts/COMP_CWORD level/value-hint kinds). zsh/fish/elvish/powershell are the deterministic generated scripts vendored verbatim as `//go:embed` parity assets (`internal/cli/completion_{zsh,fish,elvish,powershell}.txt`) — those generators carry per-flag help text / value names / per-possible-value help that exists only in codex's Rust CLI definition, so a byte-identical reproduction must embed that metadata regardless; vendoring the real deterministic output is the honest equivalent (not a hand-rolled approximation). Remaining cosmetic gap: an unsupported `SHELL` value emits clap's `error: invalid value …` body + exit 2, but the optional `tip: a similar value exists: '…'` line (clap's internal Jaro + substring "did you mean" heuristic) is omitted — a wrong suggestion is worse than none and the precise port is not worth it. | regenerate the embedded zsh/fish/elvish/powershell assets if codex's CLI tree changes; port clap's did-you-mean heuristic only if the tip line becomes load-bearing | accepted |
| 19 threadstore | behavioral | `LocalThreadStore` read/list/search/archive/resume/update now fully implemented (state-DB-first with sessions-tree scan fallback). **SearchThreads now scans rollout transcripts** (`internal/threadstore/local_search.go`), a faithful port of `thread-store/src/local/search_threads.rs::search_threads` + `rollout/src/search.rs`: phase 1 matches a rollout file when its RAW JSONL contains the JSON-escaped term (case-insensitive, literal — mirroring `scan_rollout_paths`/`rollout_contains`, the dependency-free fallback that is byte-equivalent to codex's `rg --fixed-strings --ignore-case` path); phase 2 lists threads in sort order, keeps only those whose path matched AND that yield a `first_rollout_content_match_snippet` (the 48-before/96-after-char excerpt with `... `/` ...` markers over user/agent event messages and user/assistant response `Message` items, with `USER_MESSAGE_BEGIN` stripping and whitespace normalization); paging uses the `page_size+1` overflow + next-cursor scheme. The one intentional simplification: codex prefers the external `rg` binary and falls back to this same in-process scan when `rg` is absent — codexgo always uses the in-process scan (identical match semantics, no `rg` dependency). Remaining minor: SearchThreads result NAMES are sourced from the rollout/SQLite path the existing `applyStoredThreadName` provides (codex's `set_thread_search_result_names` additionally consults `find_thread_names_by_ids` legacy index); UpdateThreadMetadata writes the SQLite row but not rollout session-meta lines | the read/list/search/archive surface is complete and tested; the `rg`-vs-in-process choice is byte-equivalent; the remaining items are dependency-gated | accepted |
| 08 login | behavioral | AWS SigV4 is an interface stub; agent-task X25519 registration network side-effects out of scope | not on the OpenAI/ChatGPT critical path; add with Bedrock support | accepted |
| 04 config | behavioral | layer-state/fingerprint/origins, macOS MDM managed prefs, cloud-requirements, git-trust project loading, keymap parsing carried as opaque TOML trees / omitted | large peripheral surface; load/merge/validate + typed schema are faithful | accepted |
| 32 app-server-protocol | behavioral | ts-rs/schemars schema-export generator deferred; a few cross-area fields carried as `json.RawMessage` | the runtime types/methods are faithful; the codegen tool is non-runtime | accepted |

| 34 exec | behavioral | **Closed.** On an API error codexgo now surfaces the upstream response body verbatim (`modelFacingErrorMessage` unwraps the typed `client.TransportError`), byte-identical to codex's UnexpectedStatus display — enforced by `TestParityTurnError`'s message assertion alongside the event shape/exit-code checks. Non-HTTP errors keep their full wrapped message (codex's other error variants are message-formatted per kind; byte parity for those is asserted as scenarios gain differentials). | extend per-error-kind differentials as scenarios land | accepted |
| 34 request `input` | protocol | **Closed for the verified scenarios.** codexgo seeds the full codex initial context: the developer message bundles `<permissions instructions>` + `<skills_instructions>` (the SKILL.md scan over the default skill roots, with the embedded system skills materialized under `CODEX_HOME/skills/.system`) and the user message carries `<environment_context>`. `TestParityInputContext` + `TestParityRequestBody` assert the whole `input` field is **byte-identical** to real codex (per-run CODEX_HOME paths normalized). Skill-root assembly now ports all four config-layer scopes (`internal/skills/roots.go` `skill_roots_from_layer_stack_inner` order): project `.codex/skills` (Repo, cwd-closest first), user (CODEX_HOME skills, `$HOME/.agents/skills`, `.system`), admin `/etc/codex/skills` (System config layer, always emitted; seam `WithSystemConfigDir` injects a writable dir in tests), then the repo `.agents/skills` chain. **STUB (trust gate):** project `.codex/skills` discovery is gated behind `WithProjectLayer`, which the headless CLI host leaves OFF because codexgo does not yet port the git-trust `ProjectTrustContext` that the Rust loader requires for project layers. Non-read-only sandbox `<filesystem>` XML is now **byte-verified**: workspace-write (managed/restricted with `:root` read, cwd write, `:slash_tmp`/`:tmpdir` write, and `{cwd}/.git`,`.agents`,`.codex` read carveouts) and danger-full-access (disabled/unrestricted) match codex 0.136.0 (`TestParityInputContextSandboxModes` runs both binaries in one cwd; extra `writable_roots` config not yet threaded). | port the git-trust gate to enable project skill roots in the host; thread `writable_roots` config into the workspace-write filesystem context | review |
| 34 request `tools` | protocol | **Closed at the request level.** codexgo advertises codex's full 11-tool default registry, byte-identical and in codex's exact spec_plan order, enforced as full-array equality (`TestParityToolSpecs` + `TestParityToolOrder`) and by `TestParityRequestBody` (the `tools` field left `documentedGapFields`). The goals trio is gated like codex's `goal_tools_enabled` (SQLite state runtime wired ∧ `Feature::Goals` ∧ not the review sub-agent) and backed by a real goal store (`internal/state/goals.go`); `tool_search` is gated like `append_tool_search_executor`. **Provider-capability gating now LIVE**: `ProviderCapabilities` is modeled (`internal/modelproviderinfo/capabilities.go`, mirroring the Rust `model-provider/src/provider.rs` defaults all-true + the Amazon Bedrock override disabling hosted `image_generation`/`web_search`), threaded through `SessionConfiguration`/`TurnContext` (`internal/core/provider_capabilities.go`, resolved from the provider id via a resolver the assembly wires to `CapabilitiesForProvider`), and read by `turnNamespaceToolsEnabled` (gates `tool_search`) and the `web_search` executor's provider gate — replacing the two STUBs. Bare-run behavior is unchanged (OpenAI provider: all true), guarded by the full parity suite; a capabilities-off provider hides `tool_search`/`web_search` (`TestProviderCapabilitiesGateHostedTools`, `TestCapabilitiesForProvider`). | none — closed | accepted |
| 34 `tool_search` dispatch | behavioral | **Closed for the bare-run multi_agent_v1 scenario.** The five collab agent tools are registered as deferred runtimes (`internal/core/collab_executors.go`), their v1 specs are ported (`internal/multiagent/spec`), and a faithful BM25 engine (`internal/tools/bm25*.go`) ranks them. A scripted `tool_search` (query "spawn agents") feeds back the coalesced `multi_agent_v1` namespace byte-identically to codex once the BM25 tie order is canonicalized (`TestParityToolSearchTurn`). **Collab execution now LIVE**: the five handlers are ported (`internal/core/collab_handlers.go` — spawn/send_input/resume/wait/close with the Rust response shapes + Collab* lifecycle events) and dispatch through the `CollabControl` seam to `internal/multiagent` (`CollabAdapter` over the real `Control` + ThreadManager, wired in the CLI assembly with a per-root control plane over a shared spawn-edge graph). Full-history `fork_context` spawns are now LIVE: the originating session's rollout path is exposed read-only (`Session.RolloutPath()`), threaded through `CollabSpawnRequest.ParentRolloutPath` (`handleSpawnAgent`), and converted by `CollabAdapter.SpawnAgent` into `SpawnOptions{Fork: FullHistory, ForkParentRolloutPath}` so the child's initial history is a full fork of the parent thread's stored rollout (`TestCollabAdapterForkContextForksParentHistory`), mirroring the Rust spawn fork path; a fork is only rejected (model-facing error) when the host has no rollout persistence for the parent (nil/empty path). **MCP `defer_loading` tools now register as deferred runtimes** (`internal/core/mcp_deferred_executor.go`, porting the deferred branch of `add_mcp_runtime_tools`): each `DeferredMcpTools` entry is dispatch-only (hidden spec), routes direct calls through the same MCP caller as the eager `mcpExecutor`, and contributes a `tool_search` entry + per-server `ToolSearchSourceInfo` collected in spec_plan's `add_collaboration_tools`→`add_mcp_runtime_tools` order (the search text + namespace spec are built in `internal/tools/mcp_search.go`, byte-identical to `McpHandler::search_info` / `build_mcp_search_text` per `TestMcpSearchInfo*`). They surface through BM25 alongside the collab entries and dedup their sources in the tool_search description (`TestDeferredMcp*`). `TestParityToolSearchTurn` stays byte-identical (the bare run wires no MCP servers, so `DeferredMcpTools` is empty). Remaining: goal-tool accounting events (`thread_goal_updated`) are still dropped in the headless bridge (nil event sink); MCP server discovery itself (populating `DeferredMcpTools`) lands with the MCP area. | MCP server discovery + event sink land with their areas | review |
| 34 `tool_search` BM25 | behavioral | The BM25 engine mirrors the `bm25` crate v2.3.2 (k1=1.2, b=0.75; IDF `ln(1+(N-df+0.5)/(df+0.5))`; saturated TF; avgdl fit) and the `DefaultTokenizer` (lowercase, UAX #29 word split via `rivo/uniseg`, NLTK English stopwords, Snowball/Porter2 English stem). The stemmer is hand-ported and validated against the **rust-stemmers v1.2.0** test vocabulary (29,417 word→stem pairs, all matching) — note rust-stemmers ships an older Porter2 variant than current snowball-data (e.g. `added`→`ad`, `dyed`→`dy`), so that data, not snowball-data, is the truth. Unicode normalization (`deunicode`) is treated as identity (the codex search texts are ASCII). The embedding space is keyed by the token string rather than the `fxhash` of it (the hash is only ever a map key; equivalent for scoring). | none expected for ASCII corpora; revisit normalization if non-ASCII search texts appear | accepted |
| 34 `tool_search` tie order | behavioral | codex's `ToolSearchHandler` collects BM25 candidates into a `std::collections::HashSet` before its stable sort, so tools with **equal** BM25 scores are emitted in a **process-random** order (the multi_agent corpus ties `close_agent`/`resume_agent` for query "spawn agents", and codex flips that pair ~50/50 run-to-run). codexgo is deterministic (descending document id). The differential (`TestParityToolSearchTurn`) compares after canonicalizing each namespace's inner-tool order by name, and separately asserts the deterministic relevance leader (`spawn_agent`) is first on both sides, so spec content and ranking are still enforced while tolerating codex's non-determinism. | not reproducible: codex itself is non-deterministic here | accepted |
| 34 collab spec home | behavioral | Multi-agent v1 spec builders live in `internal/multiagent/spec` (a leaf package), not `internal/multiagent` (the suggested home), because `internal/multiagent` imports `internal/core` while the spec builders must be importable **by** `internal/core` (the deferred collab runtimes) — placing them in `internal/multiagent` would create an import cycle. The leaf depends only on `internal/tools`+`internal/modelsmanager`. Output schemas are omitted from the builders (the tool_search `ToolSearchInfo` conversion strips `output_schema`; the v1 search specs never need it). spawn_agent's `available_models` is derived from the bundled catalog with no authenticated backend, matching codex when the models endpoint is empty and no ChatGPT auth is present. | output schemas + auth-aware model lists land if a direct (non-deferred) collab spec path is needed | accepted |
| 28 unified-exec bridge | behavioral | Reduced orchestration vs codex's ToolOrchestrator: (a) **CLOSED** — unified-exec (and the local exec service) now resolve and apply the REAL per-turn sandbox policy. `internal/core/turn_sandbox_policy.go` (`resolveTurnSandboxPolicy`) mirrors codex's `turn_context.{file_system,network}_sandbox_policy()` + `SandboxManager::select_initial(Auto)`: read-only → restricted FS (root read), workspace-write → `FileSystemSandboxPolicy::workspace_write` entries (root read + `project_roots`/`/tmp`/`$TMPDIR` write; `.git`/`.codex`/`.agents` carveouts materialized by the seatbelt backend at spawn), danger-full-access → unrestricted FS + network enabled → `none` backend. Network is restricted unless the turn grants access (always enabled for danger-full-access). `buildUnifiedExecRequest` threads `(SandboxType, FileSystemSandboxPolicy, NetworkSandboxPolicy)` into the `exec_command` request and `shellCommandExecutor.runShellCommand` threads them into `core.ExecRequest`; `localExecService.Run` spawns the child under the resolved backend via `sandbox.Spawn` (Seatbelt on macOS) for non-`none` turns and keeps the in-process `LocalProcess` path for `none`. `TurnContext.{SandboxMode,NetworkAccessEnabled}` are threaded from `SessionConfiguration` in `newTurnContext`. Verified behaviorally on darwin (`exec_service_darwin_test.go`, `unified_exec_executor_darwin_test.go`): an outside-workspace write fails under read-only and succeeds under danger-full-access. Two STUBs remain inside (a): (a1) the legacy `sandbox_workspace_write` knobs (extra `writable_roots`, `exclude_tmpdir_env_var`, `exclude_slash_tmp`) are not threaded through core's reduced config — the default workspace-write profile is used; (a2) the approval flow on a sandbox denial (codex's `request_command_approval` / escalation + per-call `sandbox_permissions`/`additional_permissions`) is owned by the approvals area and not built — the headless path has no interactive client, so the ambient turn policy is applied as the first-attempt default with no re-prompt; (b) **CLOSED** — the background exec-end watcher (`async_watcher.rs`) is ported. `internal/unifiedexec/async_watcher.go` reproduces `start_streaming_output` + `spawn_exit_watcher` (UTF-8-boundary output deltas, `TRAILING_OUTPUT_GRACE=100ms`, shared transcript, late end on exit) over the `Process` subscription surface, with no core dependency; `internal/core/unified_exec_watcher.go` supplies the event sink and arms it per session (`armSessionUnifiedExecWatcher` in `Spawn`). A persisted session that outlives its `exec_command` call now streams `exec_command_output_delta` events and emits a late `exec_command_end` tagged `unified_exec_startup` with the ORIGINATING call id, process id, aggregated transcript, exit code, and duration. Two narrow STUBs remain inside (b): (b1) the late end's `formatted_output` is the verbatim aggregated output rather than `format_exec_output_str(turn.truncation_policy)` — the originating turn's truncation policy is not threaded through the `unifiedexec` session-stored hook (matches the existing in-call `emitExecCommandEnd`); (b2) the watcher's failed-end path IS ported (it reads `Process.FailureMessage()` and emits the failed-end shape), but the upstream deferred-network-approval trigger (`terminate_process_on_network_denial` → `fail_and_terminate`, plus `network_denial_message_for_session`) is owned by the approvals area and not yet wired, so nothing currently sets a network-denial failure message on a live session; (c) **CLOSED** — `write_stdin`'s `terminal_interaction` now echoes the ORIGINATING exec_command's call id from the process entry (`ProcessEntry.call_id` → `Output.EventCallID`), like codex; (d) in ShellCommand mode `exec_command`/`write_stdin` stay registered dispatch-only (codex unregisters them) — invisible to a well-behaved model. The model-visible response format (`ExecCommandToolOutput::response_text`) is section-identical. | (a1) legacy workspace-write knobs + (a2) sandbox-denial approval flow + (b1) truncation policy + (b2) network-approval finalization land with the approvals/sandbox area specs | review |
| 44 doctor | format | **Closed for the JSON shape + key sets.** `doctor --json` now emits each check's `details` as a keyed object (label → string, or array for repeated labels), with `{}` for the empty case and a `notes` array for non-`label: value` strings — byte-identical to codex's `structured_json_details`/`JsonDoctorCheck` (`doctor_details.go`, `doctorCheck.MarshalJSON`). All 18 checks' detail key sets + value types match `/tmp/codex_doctor.json` on this machine; verified by `TestDoctorDetailKeySetsMatchCodex`. New plumbing ported faithfully: config.load feature-flag rows (enabled/overrides via `features.FromSources`) + config.toml path/parse array; runtime.provenance `commit` (ldflags `BuildCommit`, default "unknown") + descriptive install context; installation managed-package-root + `which -a codex` rows; git.environment PATH candidates + exec-path + build-options + `.git entry`; terminal.env terminal name/version/size/`color output`; terminal.title project source/value; state.paths active/archived rollout stats; updates.status `update action` + version-cache array + network-gated latest-version probe; websocket provider metadata + `auth mode` + DNS family. | **Value-fidelity gaps (key present, value best-effort):** system.environment `os`/`os type`/`os version` use sw_vers/os-release/uname (no `os_info` crate); sandbox.helpers `execve wrapper helper` is `none` (no Arg0DispatchPaths plumbing) and filesystem/network sandbox derive from `sandbox_mode` (not the full `Permissions` model); state.rollout_db_parity always takes the state-DB-missing path (no rollout/thread state DB), so `rollout DB rows` is `skipped`, malformed-name/scan-cap are `0`/`false`; websocket `handshake transport error`/`DNS`/`handshake result` come from a bounded HTTPS HEAD probe + independent DNS lookup, not codex's live Responses-WS handshake (`handshake result` vs `handshake transport error` is probe-outcome-conditional). Environment-conditional keys (reachability label by auth mode, proxy/terminal/install rows) match under the same environment. | review |

> Entries are appended as each spec lands and reports its deviations.
> **`review (must finish)`** items are genuine gaps to close before claiming the
> corresponding spec complete — they are not permanent accepted deviations.
