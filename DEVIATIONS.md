# Deviations from Codex 0.136.0

The single, reviewed registry of every place codexgo intentionally diverges from the
behavior or output of the reference Codex build. The parity goal is *zero unexplained*
diffs — every entry below has a rationale. **Material** deviations (user-visible
behavior or ecosystem interop) are marked `review` and need maintainer sign-off
(see `docs/ROADMAP.md` §8); non-material ones are `accepted`.

Categories: `format` (on-disk), `protocol` (wire), `behavioral`, `cosmetic`.

| Spec | Category | Deviation | Rationale | Status |
|------|----------|-----------|-----------|--------|
| 01 abspath | behavioral | `dunce` crate → stdlib `filepath` + hand-rolled verbatim/UNC prefix stripping | Go already emits friendly Windows paths; verbatim stripping replicated in `normalizeWindowsDevicePath` | accepted |
| 01 abspath | behavioral | serde thread-local base-path guard → explicit base param (`Unmarshal(data, base)` / `Decoder`) | Go has no thread-locals; explicit base is idiomatic and equivalent | accepted |
| 01 abspath/pathutil | behavioral | `Path::canonicalize` → `filepath.EvalSymlinks`+`Abs` | closest stdlib analogue; macOS `/var`→`/private/var` may differ on some paths | accepted |
| 01 fuzzymatch / 20 filesearch | behavioral | `nucleo` ranking → custom `fuzzymatch` scorer | exact nucleo scores are impractical to reproduce; result **set** preserved, **order** may differ slightly | review |
| 20 filesearch | behavioral | `ignore` crate → hand-rolled `.gitignore` matcher | supports comments/negation/anchoring/`**`/`[...]`; not bit-exact to git for exotic escapes | review |
| 20 filesearch | behavioral | async streaming search session omitted; only synchronous `Run` ported | `Run` is the value-producing entry point; streaming session can be added later | accepted |
| 20 filewatcher | behavioral | `notify` recursive watch → fsnotify (non-recursive OS watch; recursive flag is semantic-only) | fsnotify has no recursive `Add`; synthetic events + direct children match codex; deep recursive OS delivery is platform-dependent | review |
| 20 filewatcher | behavioral | RAII `Drop` → explicit idempotent `Close()`; tokio `Notify` → `sync.Cond` | Go has no destructors/async runtime; semantics preserved | accepted |
| cross-cutting | behavioral | tokio async (channels, `Arc<Mutex>`, `CancellationToken`) → goroutines/channels/`sync`/`context` | language-idiomatic concurrency; observable ordering preserved | accepted |
| cross-cutting | format | JSON object key ordering (`encoding/json` sorts map keys) | parity asserted via canonicalizer (`internal/paritytest`); struct field order is controlled explicitly where it matters | accepted |
| 02 protocol | format | internally/externally-tagged serde enums → single struct + `Kind` discriminator + custom `MarshalJSON`/`UnmarshalJSON` | Go lacks sum types; wire JSON is preserved exactly | accepted |
| 02 protocol | cosmetic | `op.go` is 1193 lines (> 800 guideline) | `Op` marshal/unmarshal form one cohesive 25-variant unit; splitting hurts cohesion | accepted |
| 03 execpolicy | behavioral | **`network_rule` landed.** The Starlark `network_rule(host, protocol, decision, justification)` builtin + `NetworkRule`/`NetworkRuleProtocol` types + host normalization (`normalize_network_rule_host`: port/bracketed-IPv6 stripping, trailing-dot trim, lowercase, wildcard/whitespace/scheme rejection) + protocol aliasing (`https_connect`/`http-connect`→https) + `deny`→forbidden decision alias + `Policy.CompiledNetworkDomains` (ordered allow/deny upsert with prompt-ignored) + `Policy.AddNetworkRule`/`MergeOverlay` are ported faithfully (`internal/execpolicy/{network_rule,policy,builder,parser}.go`), with the Rust unit tests ported (`network_rule_test.go`). `amend.rs` (and the clap CLI) remain omitted. **The exec-policy → network-proxy wiring is now ported** (`internal/networkproxy/execpolicy.go::ApplyExecPolicyNetworkRules`), a faithful port of `apply_exec_policy_network_rules` (core/src/network_proxy_loader.rs + core/src/config/network_proxy_spec.rs): the compiled `(allowed, denied)` host lists from `Policy.CompiledNetworkDomains` are upserted onto a `NetworkProxyConfig` AFTER the config-sourced domain entries (so an exec-policy disposition wins over a config entry for the same normalized host), with allowed→Allow / denied→Deny and the Rust verbatim-host `HashSet` dedup guard reproduced. Verified end-to-end (`execpolicy_test.go`): a `network_rule(decision="allow")` in an execpolicy file permits that host through the proxy `hostBlocked` policy, an exec-policy `deny` flips a config-allowed host, and normalized-host collisions collapse to the latest entry. `amend.rs` config-rewrite remains omitted. | port `amend.rs` if config-rewrite of network rules becomes needed | review |
| 03 execpolicy | behavioral | Starlark f-strings not enabled (`go.starlark.net` has none) | codex's bundled policies use no f-strings | accepted |
| 12 sandbox | behavioral | macOS seatbelt complete (.sbpl generation + `Backend` + `sandbox-exec` spawn + policy model/matrix/resolution, with golden + darwin behavioral tests). Linux/Windows backends return not-implemented | Linux/Windows are separate XL specs 13/14; macOS is the dev/parity target for the `codex exec` milestone | accepted |
| 25 code-mode | behavioral | Complete: CodeModeService/Session engine + exec/wait on goja + cell isolation + ToolInvoker bridge. goja is ES5.1+/partial-ES2015+ vs V8 | goja is cgo-free (project goal); JS-feature gaps documented; feature-flagged off by default | accepted |
| 41/42 CLI | behavioral | Default (no subcommand) prints "TUI not yet implemented" + exit 1 (until TUI lands); archive/unarchive, mcp add/remove, debug prompt-input, non-stdio app-server transports give a clear notice + non-zero exit; exec/mcp-server run against the mock model until a real provider client is wired. **`login --device-auth` is now wired** (see spec 08 row): it runs the real device-code flow against the default issuer/client-id. **The `--experimental_issuer` / `--experimental_client-id` overrides are now parsed and threaded** (`internal/cli/cmd_login.go`): both space-separated and inline (`=`) forms are accepted, and `deviceCodeServerOptions` applies them to the device-code `ServerOptions` (client id falls back to `login.ClientID`, issuer replaced only when overridden) — mirroring `run_login_with_device_code`, which threads them into `ServerOptions` for the device-code flow only. Per Rust parity, the default browser flow (`run_login_with_chatgpt`) does NOT receive these overrides (main.rs drops them on that path), so codexgo keeps the browser path on the defaults; the flags are hidden from help (`hide=true`) exactly like Rust. Verified (`cmd_login_experimental_test.go`): the overridden issuer reaches the device-code endpoint URLs (`/api/accounts/deviceauth/usercode`, verification URL) and the client id appears in the usercode body, and `BuildAuthorizeURL` honors an overridden issuer. | turn-driving + diagnostic surface is complete; the rest are clear notices (never silent) and land with TUI / real-model wiring | review |
| 41 completion | behavioral | **Closed (byte-identical).** `codex completion {bash,elvish,fish,powershell,zsh}` is now byte-for-byte identical to the reference binary, asserted by the env-gated `TestParityCompletion` (all 5 shells + the default-is-bash case). bash is a faithful port of clap_complete v4.5.65's `bash.rs` generator (header / for-loop dispatch / `subcommand_details` / `option_details_for_path` templates) driven by an embedded command-tree model (`internal/cli/completion_tree.go`, extracted byte-exact from codex's output: 235 nodes, per-node opts/COMP_CWORD level/value-hint kinds). zsh/fish/elvish/powershell are the deterministic generated scripts vendored verbatim as `//go:embed` parity assets (`internal/cli/completion_{zsh,fish,elvish,powershell}.txt`) — those generators carry per-flag help text / value names / per-possible-value help that exists only in codex's Rust CLI definition, so a byte-identical reproduction must embed that metadata regardless; vendoring the real deterministic output is the honest equivalent (not a hand-rolled approximation). Remaining cosmetic gap: an unsupported `SHELL` value emits clap's `error: invalid value …` body + exit 2, but the optional `tip: a similar value exists: '…'` line (clap's internal Jaro + substring "did you mean" heuristic) is omitted — a wrong suggestion is worse than none and the precise port is not worth it. | regenerate the embedded zsh/fish/elvish/powershell assets if codex's CLI tree changes; port clap's did-you-mean heuristic only if the tip line becomes load-bearing | accepted |
| 19 threadstore | behavioral | `LocalThreadStore` read/list/search/archive/resume/update now fully implemented (state-DB-first with sessions-tree scan fallback). **SearchThreads now scans rollout transcripts** (`internal/threadstore/local_search.go`), a faithful port of `thread-store/src/local/search_threads.rs::search_threads` + `rollout/src/search.rs`: phase 1 matches a rollout file when its RAW JSONL contains the JSON-escaped term (case-insensitive, literal — mirroring `scan_rollout_paths`/`rollout_contains`, the dependency-free fallback that is byte-equivalent to codex's `rg --fixed-strings --ignore-case` path); phase 2 lists threads in sort order, keeps only those whose path matched AND that yield a `first_rollout_content_match_snippet` (the 48-before/96-after-char excerpt with `... `/` ...` markers over user/agent event messages and user/assistant response `Message` items, with `USER_MESSAGE_BEGIN` stripping and whitespace normalization); paging uses the `page_size+1` overflow + next-cursor scheme. The one intentional simplification: codex prefers the external `rg` binary and falls back to this same in-process scan when `rg` is absent — codexgo always uses the in-process scan (identical match semantics, no `rg` dependency). **UpdateThreadMetadata now appends rollout session-meta lines** for legacy-compatibility patches (`internal/threadstore/local_rollout_meta.go`), a port of the rollout-compat section of `thread-store/src/local/update_thread_metadata.rs`: after the SQLite row is written, a memory-mode patch appends a `RolloutItem::SessionMeta` line carrying the new memory mode with no git marker (mirroring `apply_thread_memory_mode`, so the replay code preserves the latest prior git marker), and a git-info patch appends one carrying the resolved git info (read back from the just-updated SQLite row so partial patches preserve unspecified fields) plus the current memory mode (mirroring `apply_thread_git_info_to_rollout`); a combined patch appends both (memory mode then git info); a session-meta id mismatch fails with the Rust `metadata id mismatch` internal error; the `needs_rollout_compatibility_update`/`has_observed_metadata_facts` gates are reproduced so observed-metadata syncs do not append spurious lines. Verified (`local_statedb_test.go`) by asserting the appended line's exact JSON shape (`type=session_meta`, `payload.id`, `payload.memory_mode`, `payload.git.{commit_hash,branch,repository_url}`, git omitted on memory-mode-only). Remaining minor: SearchThreads result NAMES are sourced from the rollout/SQLite path the existing `applyStoredThreadName` provides (codex's `set_thread_search_result_names` additionally consults `find_thread_names_by_ids` legacy index); a name-only patch updates the SQLite title but not the rollout thread-name index (`append_thread_name`, a separate name-index file — not a session-meta line — whose helper is not yet ported). | the read/list/search/archive/metadata surface is complete and tested; the `rg`-vs-in-process choice is byte-equivalent; the remaining items are dependency-gated | accepted |
| 08 login | behavioral | **Device-auth closed.** `codex login --device-auth` is a faithful port of `login/src/device_code_auth.rs` (`internal/login/devicecode.go`): POST `{issuer}/api/accounts/deviceauth/{usercode,token}` with the exact request bodies, the polling state machine (success / 403\|404 retry on the server interval capped at 15 min / other → `device auth failed with status {code} {reason}`; timeout → `device auth timed out after 15 minutes`; usercode 404 → the byte-identical "device code login is not enabled…" message exposed via `IsDeviceCodeNotEnabled`), the byte-identical sign-in prompt (ANSI codes + `CARGO_PKG_VERSION`=0.136.0), workspace-restriction enforcement, and the same `auth.json` persistence (`api_key=None`, no API-key exchange) as the browser flow. Tests mirror `login/tests/suite/device_code_login.rs`. AWS SigV4 remains an interface stub; agent-task X25519 registration network side-effects out of scope | not on the OpenAI/ChatGPT critical path; add with Bedrock support | accepted |
| 15 network proxy | behavioral | **HTTPS MITM data-path closed (core).** The CONNECT-termination data path is a faithful port of `network-proxy/src/{certs,mitm}.rs` (`internal/networkproxy/mitm{ca,data,connect}.go`): a per-install managed CA under `$CODEX_HOME/proxy/{ca.pem,ca.key}` (ECDSA P-256, CN `network_proxy MITM CA`, create-new/no-overwrite, 0600 key + symlink/perm validation), per-host leaf minting (IP→IP-SAN, host→DNS-SAN, ServerAuth EKU, h2+http/1.1 ALPN), CONNECT interception that terminates TLS with the minted leaf when the inner request needs inspection (limited mode or a hooked host) and blind-tunnels otherwise (mirroring `http_connect_proxy`), `evaluate_mitm_policy` parity (host-mismatch 400, DNS-rebinding local recheck, hook match/strip+inject/deny, limited-mode method clamp — each with the exact `x-proxy-error`/blocked-message/telemetry), and streaming request/response bodies upstream over a custom-CA-aware transport. Tests mirror `mitm_tests.rs` plus end-to-end real-TLS CONNECT cases. Intentional simplifications vs Rust/rama: HTTP/2 inner serving relies on Go's `net/http` over the terminated conn (rama uses `HttpServer::auto`); `MITM_INSPECT_BODIES` is `false` upstream so its body-logging path is a no-op and not reproduced. | the inner-request policy + cert lifecycle are faithful; body-inspection logging lands only if codex flips `MITM_INSPECT_BODIES` | accepted |
| 04 config | behavioral | layer-state/fingerprint/origins, macOS MDM managed prefs, cloud-requirements, keymap parsing carried as opaque TOML trees / omitted. **git-trust project decision now ported** (`internal/config/trust.go`): `BuildProjectTrustContext`/`DecisionForDir`/`IsProjectTrusted` mirror the Rust `ProjectTrustContext` — the `[projects."<key>"] trust_level` table is read from the merged config, keys are canonical (symlink-resolved, dunce-equivalent) + logical with Windows case-folding, and the lookup precedence is dir keys → project-root keys → repo-root keys (worktree-aware repo root via `gitutils.ResolveRootGitProjectForTrust`). What is NOT yet ported: gating the project *config* layers themselves on the decision (`load_project_layers` disabled-reason/strict handling) — the decision is currently consumed only by the skills host (spec 23) to gate project `.codex/skills`. | thread the trust decision into project config-layer loading (disabled-reason + strict gating) when the config-layer stack lands | review |
| 22 plugins | behavioral | **Remote-sync orchestration landed.** Two orchestration layers from the Rust `core-plugins` crate are ported over codexgo's existing manifest/install/list machinery: (1) **curated-plugins startup sync** (`internal/plugins/startup_sync{,_transport}.go`, port of `startup_sync.rs`): the `SyncOpenAIPluginsRepo` entry point runs the git→GitHub-HTTP→export-archive fallback chain, each transport's failure isolated (git→http→archive), the sha-based update decision (`.tmp/plugins.sha` compare → skip), the GitHub-zipball top-level-prefix-stripping extractor, the atomic staged-swap activation with rollback, and the local-snapshot gate (`HasLocalCuratedPluginsSnapshot`) that limits the lagging export archive to bootstrap-only. The transport is a `CuratedSyncTransport` seam (httptest + temp-dir fixtures in tests). (2) **configured-git-marketplace upgrade** (`marketplace_upgrade{,_git}.go`, port of `marketplace_upgrade.rs`+activation/git): the `UpgradeConfiguredGitMarketplaces` entry point selects git marketplaces from `[marketplaces]`, the per-marketplace update decision (remote-revision compare + install-metadata match → skip), failure isolation (each marketplace's error collected, never aborting the rest), atomic activation + `.codex-marketplace-install.json` metadata, all over a `MarketplaceGitClient` seam. Wired into `codex plugin marketplace upgrade [NAME]` (CLI surface). What is NOT ported: the app-server startup-spawn trigger (marker-file + auth-gated `tokio::spawn`) and the ChatGPT remote-installed-plugin *bundle* sync (`remote_installed_plugin_sync.rs`, auth/marketplace-endpoint heavy) — both depend on app-server/auth wiring out of this scope; otel sync metrics are no-ops (codexgo's otel layer). | trigger the curated sync at app-server startup once the auth-gated spawn lands; port the remote installed-plugin bundle sync with the ChatGPT plugin endpoints | review |
| 32 app-server-protocol | behavioral | ts-rs/schemars schema-export generator deferred; a few cross-area fields carried as `json.RawMessage` | the runtime types/methods are faithful; the codegen tool is non-runtime | accepted |

| 34 exec | behavioral | **Closed.** On an API error codexgo now surfaces the upstream response body verbatim (`modelFacingErrorMessage` unwraps the typed `client.TransportError`), byte-identical to codex's UnexpectedStatus display — enforced by `TestParityTurnError`'s message assertion alongside the event shape/exit-code checks. Non-HTTP errors keep their full wrapped message (codex's other error variants are message-formatted per kind; byte parity for those is asserted as scenarios gain differentials). | extend per-error-kind differentials as scenarios land | accepted |
| 34 request `input` | protocol | **Closed for the verified scenarios.** codexgo seeds the full codex initial context: the developer message bundles `<permissions instructions>` + `<skills_instructions>` (the SKILL.md scan over the default skill roots, with the embedded system skills materialized under `CODEX_HOME/skills/.system`) and the user message carries `<environment_context>`. `TestParityInputContext` + `TestParityRequestBody` assert the whole `input` field is **byte-identical** to real codex (per-run CODEX_HOME paths normalized). Skill-root assembly now ports all four config-layer scopes (`internal/skills/roots.go` `skill_roots_from_layer_stack_inner` order): project `.codex/skills` (Repo, cwd-closest first), user (CODEX_HOME skills, `$HOME/.agents/skills`, `.system`), admin `/etc/codex/skills` (System config layer, always emitted; seam `WithSystemConfigDir` injects a writable dir in tests), then the repo `.agents/skills` chain. **Trust gate landed:** project `.codex/skills` discovery is gated behind `WithProjectLayer`, which the headless CLI host now enables per-cwd via the ported git-trust decision (`config.IsProjectTrusted`, see spec 04 row) — only trusted projects load project skills; untrusted/no-entry projects keep them off. This is parity-neutral (the harness cwds carry no `.codex/skills`, so the byte-identical `input` differential is unaffected, re-verified by the full parity suite). Non-read-only sandbox `<filesystem>` XML is now **byte-verified**: workspace-write (managed/restricted with `:root` read, cwd write, `:slash_tmp`/`:tmpdir` write, and `{cwd}/.git`,`.agents`,`.codex` read carveouts) and danger-full-access (disabled/unrestricted) match codex 0.136.0 (`TestParityInputContextSandboxModes` runs both binaries in one cwd; extra `writable_roots` config not yet threaded). | thread `writable_roots` config into the workspace-write filesystem context | review |
| 34 request `tools` | protocol | **Closed at the request level.** codexgo advertises codex's full 11-tool default registry, byte-identical and in codex's exact spec_plan order, enforced as full-array equality (`TestParityToolSpecs` + `TestParityToolOrder`) and by `TestParityRequestBody` (the `tools` field left `documentedGapFields`). The goals trio is gated like codex's `goal_tools_enabled` (SQLite state runtime wired ∧ `Feature::Goals` ∧ not the review sub-agent) and backed by a real goal store (`internal/state/goals.go`); `tool_search` is gated like `append_tool_search_executor`. **Provider-capability gating now LIVE**: `ProviderCapabilities` is modeled (`internal/modelproviderinfo/capabilities.go`, mirroring the Rust `model-provider/src/provider.rs` defaults all-true + the Amazon Bedrock override disabling hosted `image_generation`/`web_search`), threaded through `SessionConfiguration`/`TurnContext` (`internal/core/provider_capabilities.go`, resolved from the provider id via a resolver the assembly wires to `CapabilitiesForProvider`), and read by `turnNamespaceToolsEnabled` (gates `tool_search`) and the `web_search` executor's provider gate — replacing the two STUBs. Bare-run behavior is unchanged (OpenAI provider: all true), guarded by the full parity suite; a capabilities-off provider hides `tool_search`/`web_search` (`TestProviderCapabilitiesGateHostedTools`, `TestCapabilitiesForProvider`). | none — closed | accepted |
| 34 `tool_search` dispatch | behavioral | **Closed for the bare-run multi_agent_v1 scenario.** The five collab agent tools are registered as deferred runtimes (`internal/core/collab_executors.go`), their v1 specs are ported (`internal/multiagent/spec`), and a faithful BM25 engine (`internal/tools/bm25*.go`) ranks them. A scripted `tool_search` (query "spawn agents") feeds back the coalesced `multi_agent_v1` namespace byte-identically to codex once the BM25 tie order is canonicalized (`TestParityToolSearchTurn`). **Collab execution now LIVE**: the five handlers are ported (`internal/core/collab_handlers.go` — spawn/send_input/resume/wait/close with the Rust response shapes + Collab* lifecycle events) and dispatch through the `CollabControl` seam to `internal/multiagent` (`CollabAdapter` over the real `Control` + ThreadManager, wired in the CLI assembly with a per-root control plane over a shared spawn-edge graph). Full-history `fork_context` spawns are now LIVE: the originating session's rollout path is exposed read-only (`Session.RolloutPath()`), threaded through `CollabSpawnRequest.ParentRolloutPath` (`handleSpawnAgent`), and converted by `CollabAdapter.SpawnAgent` into `SpawnOptions{Fork: FullHistory, ForkParentRolloutPath}` so the child's initial history is a full fork of the parent thread's stored rollout (`TestCollabAdapterForkContextForksParentHistory`), mirroring the Rust spawn fork path; a fork is only rejected (model-facing error) when the host has no rollout persistence for the parent (nil/empty path). **MCP `defer_loading` tools now register as deferred runtimes** (`internal/core/mcp_deferred_executor.go`, porting the deferred branch of `add_mcp_runtime_tools`): each `DeferredMcpTools` entry is dispatch-only (hidden spec), routes direct calls through the same MCP caller as the eager `mcpExecutor`, and contributes a `tool_search` entry + per-server `ToolSearchSourceInfo` collected in spec_plan's `add_collaboration_tools`→`add_mcp_runtime_tools` order (the search text + namespace spec are built in `internal/tools/mcp_search.go`, byte-identical to `McpHandler::search_info` / `build_mcp_search_text` per `TestMcpSearchInfo*`). They surface through BM25 alongside the collab entries and dedup their sources in the tool_search description (`TestDeferredMcp*`). `TestParityToolSearchTurn` stays byte-identical (the bare run wires no MCP servers, so `DeferredMcpTools` is empty). **Goal event sink now LIVE**: goal-tool accounting events (`thread_goal_updated`) are no longer dropped — the CLI assembly wires a real `extensionapi.ExtensionEventSink` (`internal/cli/goal_event_sink.go`) into `goal.NewToolExecutors`. Because the goal executors are built per-thread in the `ToolRouterFactory` BEFORE the session exists, the sink resolves the owning session lazily at `Emit` time through a shared late-bound `threadManagerHolder` (the same holder pattern the multi-agent collab engine uses), then forwards the pre-built event verbatim (preserving its call-id correlation) onto the session event stream via the new `Session.EmitEvent`. The Go analogue of the Rust `AppServerExtensionEventSink` (which forwards `ThreadGoalUpdated` to the outgoing notification channel). Verified end-to-end: `create_goal` during a turn emits `thread_goal_updated` on the session stream (`TestRunSamplingRequestCreateGoalEmitsThreadGoalUpdated`). The exec JSONL processor does NOT map `ThreadGoalUpdated` — matching codex's `event_processor_with_jsonl_output` (`ThreadGoalUpdated` is a global notification, not a thread item, so it falls through the `_ => CodexStatus::Running` arm and produces no JSONL line). Remaining: MCP server discovery itself (populating `DeferredMcpTools`) lands with the MCP area. | MCP server discovery lands with the MCP area | review |
| 34 `tool_search` BM25 | behavioral | The BM25 engine mirrors the `bm25` crate v2.3.2 (k1=1.2, b=0.75; IDF `ln(1+(N-df+0.5)/(df+0.5))`; saturated TF; avgdl fit) and the `DefaultTokenizer` (lowercase, UAX #29 word split via `rivo/uniseg`, NLTK English stopwords, Snowball/Porter2 English stem). The stemmer is hand-ported and validated against the **rust-stemmers v1.2.0** test vocabulary (29,417 word→stem pairs, all matching) — note rust-stemmers ships an older Porter2 variant than current snowball-data (e.g. `added`→`ad`, `dyed`→`dy`), so that data, not snowball-data, is the truth. Unicode normalization (`deunicode`) is treated as identity (the codex search texts are ASCII). The embedding space is keyed by the token string rather than the `fxhash` of it (the hash is only ever a map key; equivalent for scoring). | none expected for ASCII corpora; revisit normalization if non-ASCII search texts appear | accepted |
| 34 `tool_search` tie order | behavioral | codex's `ToolSearchHandler` collects BM25 candidates into a `std::collections::HashSet` before its stable sort, so tools with **equal** BM25 scores are emitted in a **process-random** order (the multi_agent corpus ties `close_agent`/`resume_agent` for query "spawn agents", and codex flips that pair ~50/50 run-to-run). codexgo is deterministic (descending document id). The differential (`TestParityToolSearchTurn`) compares after canonicalizing each namespace's inner-tool order by name, and separately asserts the deterministic relevance leader (`spawn_agent`) is first on both sides, so spec content and ranking are still enforced while tolerating codex's non-determinism. | not reproducible: codex itself is non-deterministic here | accepted |
| 34 collab spec home | behavioral | Multi-agent v1 spec builders live in `internal/multiagent/spec` (a leaf package), not `internal/multiagent` (the suggested home), because `internal/multiagent` imports `internal/core` while the spec builders must be importable **by** `internal/core` (the deferred collab runtimes) — placing them in `internal/multiagent` would create an import cycle. The leaf depends only on `internal/tools`+`internal/modelsmanager`. Output schemas are omitted from the builders (the tool_search `ToolSearchInfo` conversion strips `output_schema`; the v1 search specs never need it). spawn_agent's `available_models` is derived from the bundled catalog with no authenticated backend, matching codex when the models endpoint is empty and no ChatGPT auth is present. | output schemas + auth-aware model lists land if a direct (non-deferred) collab spec path is needed | accepted |
| 28 unified-exec bridge | behavioral | Reduced orchestration vs codex's ToolOrchestrator: (a) **CLOSED** — unified-exec (and the local exec service) now resolve and apply the REAL per-turn sandbox policy. `internal/core/turn_sandbox_policy.go` (`resolveTurnSandboxPolicy`) mirrors codex's `turn_context.{file_system,network}_sandbox_policy()` + `SandboxManager::select_initial(Auto)`: read-only → restricted FS (root read), workspace-write → `FileSystemSandboxPolicy::workspace_write` entries (root read + `project_roots`/`/tmp`/`$TMPDIR` write; `.git`/`.codex`/`.agents` carveouts materialized by the seatbelt backend at spawn), danger-full-access → unrestricted FS + network enabled → `none` backend. Network is restricted unless the turn grants access (always enabled for danger-full-access). `buildUnifiedExecRequest` threads `(SandboxType, FileSystemSandboxPolicy, NetworkSandboxPolicy)` into the `exec_command` request and `shellCommandExecutor.runShellCommand` threads them into `core.ExecRequest`; `localExecService.Run` spawns the child under the resolved backend via `sandbox.Spawn` (Seatbelt on macOS) for non-`none` turns and keeps the in-process `LocalProcess` path for `none`. `TurnContext.{SandboxMode,NetworkAccessEnabled}` are threaded from `SessionConfiguration` in `newTurnContext`. Verified behaviorally on darwin (`exec_service_darwin_test.go`, `unified_exec_executor_darwin_test.go`): an outside-workspace write fails under read-only and succeeds under danger-full-access. Two STUBs remain inside (a): (a1) the legacy `sandbox_workspace_write` knobs (extra `writable_roots`, `exclude_tmpdir_env_var`, `exclude_slash_tmp`) are not threaded through core's reduced config — the default workspace-write profile is used; (a2) **CLOSED** — the sandbox-denial approval escalation is ported. `internal/core/sandbox_escalation.go` mirrors the `SandboxErr::Denied` arm of `ToolOrchestrator::run` for the non-network FS denial path: `isLikelySandboxDenied` reproduces `is_likely_sandbox_denied` (keyword scan + quick-reject exit codes 2/126/127 + Linux seccomp SIGSYS at 128+31), `resolveSandboxEscalation` applies `wantsNoSandboxApproval`/`shouldBypassNoSandboxApproval`/`unsandboxedExecutionAllowed` (Never/OnRequest surface the denial; OnFailure/UnlessTrusted/Granular-with-sandbox-approval prompt via `Session.RequestCommandApproval` with the terse `"command failed; retry without sandbox?"` reason; denied reads forbid the unsandboxed retry). On approval the command re-runs with `SandboxType::None`: `shellCommandExecutor.runShellWithEscalation` re-runs through the ExecService, and `unifiedExecCommandExecutor.maybeEscalateUnifiedExec` reserves a fresh process id and re-spawns (`ForceSandboxNone`). begin/end events are emitted once each (the end reflects the FINAL attempt), matching the handler `begin → orchestrator.run → finish` ordering. The headless path resolves the prompt as deny via the pre-closed-waiter fallback (no interactive client), mirroring headless `request_user_input`. Verified by core tests (`TestResolveSandboxEscalation`, `TestShellCommandEscalation*`, `TestIsLikelySandboxDenied`) and end-to-end on the real Seatbelt backend (`TestUnifiedExecEscalationApproveRetriesUnsandboxed`). Under `approval_policy=never`/danger-full-access (the differential suite) the first attempt is `none`, so no escalation runs — byte-identical. Still STUB inside (a2): per-call `sandbox_permissions`/`additional_permissions`, the ApprovedForSession cache, guardian routing, and network-policy denial escalation (owned by the permissions/guardian/network areas); (b) **CLOSED** — the background exec-end watcher (`async_watcher.rs`) is ported. `internal/unifiedexec/async_watcher.go` reproduces `start_streaming_output` + `spawn_exit_watcher` (UTF-8-boundary output deltas, `TRAILING_OUTPUT_GRACE=100ms`, shared transcript, late end on exit) over the `Process` subscription surface, with no core dependency; `internal/core/unified_exec_watcher.go` supplies the event sink and arms it per session (`armSessionUnifiedExecWatcher` in `Spawn`). A persisted session that outlives its `exec_command` call now streams `exec_command_output_delta` events and emits a late `exec_command_end` tagged `unified_exec_startup` with the ORIGINATING call id, process id, aggregated transcript, exit code, and duration. One narrow STUB remains inside (b): (b1) **CLOSED** — the late end's `formatted_output` now formats via `format_exec_output_str(turn.truncation_policy)`. The originating turn's truncation policy (`turnTruncationPolicy`) is threaded through the `unifiedexec` session-stored hook: `ExecCommandRequest.TruncationPolicy` → `StoredSession.TruncationPolicy` → `unifiedExecWatcherSink.truncationPolicy`, and `ExecEnd` computes `FormattedOutput = truncation.FormattedTruncateText(aggregated, policy)` (timed_out is always false for the watcher, so `build_content_with_timeout` is just the aggregated output) for both the success and failed-end arms (the Rust emitter routes both through the same `format_exec_output_str` path). Verified (`unified_exec_watcher_test.go`): a small byte policy truncates a large transcript while the aggregated output stays verbatim, and the failed-end arm truncates `stdout\nmessage` with the policy. (b2) the watcher's failed-end path IS ported (it reads `Process.FailureMessage()` and emits the failed-end shape), but the upstream deferred-network-approval trigger (`terminate_process_on_network_denial` → `fail_and_terminate`, plus `network_denial_message_for_session`) is owned by the approvals area and not yet wired, so nothing currently sets a network-denial failure message on a live session; (c) **CLOSED** — `write_stdin`'s `terminal_interaction` now echoes the ORIGINATING exec_command's call id from the process entry (`ProcessEntry.call_id` → `Output.EventCallID`), like codex; (d) in ShellCommand mode `exec_command`/`write_stdin` stay registered dispatch-only (codex unregisters them) — invisible to a well-behaved model. The model-visible response format (`ExecCommandToolOutput::response_text`) is section-identical. | (a1) legacy workspace-write knobs + (a2) per-call sandbox_permissions / ApprovedForSession cache / guardian / network-denial escalation + (b2) network-approval finalization land with the approvals/sandbox/permissions area specs | review |
| 36–40 TUI first-frame | cosmetic | **Wave 2: inline-scrollback architecture + composer/footer alignment landed — the idle first frame is now cell-identical end-to-end.** Building on wave 1's frame-differential harness (`internal/paritytest/tui_frame_test.go` + `tui_frame_helpers_test.go`; PTY-driven 80×24 + 120×40, hermetic CODEX_HOME with the cwd pre-trusted, the in-repo VT100 emulator `vtgrid.go`, row-by-row diff gated by `CODEX_TUI_FRAME_STRICT=1`), codexgo's chat surface now matches codex's INLINE ratatui model. **(A) Inline scrollback:** the bubbletea program runs WITHOUT the alternate screen (`internal/tui/run.go` drops `tea.WithAltScreen`, sets `ModelConfig.Inline`). Finalized history cells are written into the terminal's NATIVE SCROLLBACK via `tea.Println` (`Model.withScrollback` → `ChatTranscript.DrainScrollback`, the new `ScrollbackDrainer` seam in `layout.go`), with a single blank line BETWEEN cells (port of `app/resize_reflow.rs` `display_lines_for_history_insert`). The live viewport renders only the still-active transcript tail, a one-row top inset (codex's bottom-pane `inset(top=1)`, `chatwidget/rendering.rs`), and the composer/status block (`Model.inlineView`). On a fresh session the session-header card is drained to scrollback on the first resize, so the live region is exactly the 4-row composer block. **(B) Composer body + footer:** `internal/tui/chat_bottom.go` now renders codex's bordered-LESS composer (`chat_composer.rs` `Block::default().style(...)`, no `Borders`): a blank top-pad row, the `› <text>` prompt row (or the dim idle placeholder), a blank bottom-pad row (the `Min(3)` block with textarea inset top=1/bottom=1), then the default status-line footer `model-with-reasoning · current-dir` (`composer_footer.go`, ports `DEFAULT_STATUS_LINE_ITEMS` + `status_line_from_segments` join `" · "` + `truncate_line_with_ellipsis_if_overflow`; reasoning effort defaults to `default`). The idle placeholder is one of the 8-entry `PLACEHOLDERS` pool (`composer_placeholder.go`, mirrors `chatwidget.rs`), picked per-run at random exactly like codex (`chatwidget/constructor.rs`). **Result (strict):** every non-masked row is byte-identical at both sizes — header card, top inset, composer pad rows, the footer's `<model> <reasoning> · ` prefix (verified when both share a short cwd), and all blank rows. **Tooltip handling:** the harness disables the startup tooltip for BOTH binaries (`writeFrameParityConfig` sets `tui.show_tooltips = false`) because codex's tip is a network-fetched, release- and time-volatile RANDOM announcement (`tui/src/tooltips.rs`) that cannot be reproduced byte-for-byte; with it off the scrollback is fully deterministic. **Wave 3: SGR-attribute comparison + real version + dynamic frame.** **(A) SGR attributes:** `vtgrid.go` now RECORDS per-cell attributes (fg/bg incl. 16-color, 256-indexed, 24-bit RGB; bold/dim/italic/underline/reverse) instead of collect-and-drop — named ANSI colors normalize to their 0..15 index so `SGR 36` and `38;5;6` both compare as cyan, making escape-ordering irrelevant. A second assertion layer (`diffGridAttrs`) runs on the char-matched, non-masked rows under `CODEX_TUI_FRAME_STRICT=2` (a strict superset of `=1`). codexgo's styling was aligned to codex's actual captured SGR: the header `/model` hint uses ratatui's NAMED `Color::Cyan` (SGR 36, `tui.ANSICyan`) not the theme info accent (was `38;5;116`); the composer prompt `›` is BOLD-only with an unstyled gutter space (was primary-fg+bold on `"› "`); the idle placeholder is bare DIM/SGR 2 (`lipglossDim` now `Faint(true)`, was a dim-colored fg). **Result: 0 cell-attribute mismatches** across ~1680 (80×24) / ~4440 (120×40) compared cells at strict level 2. **(B) Real version:** `tui.CodexVersion = "0.136.0"` (the upstream codex version codexgo impersonates) is threaded into `RunConfig.Version` (default + `cmd_default.go`); the header title row `│ >_ OpenAI Codex (v0.136.0) … │` now matches byte-for-byte and is **NO LONGER masked**. `cli.Version` (build identity, `0.0.0-dev`) is intentionally left distinct — `--version`/doctor parity is a separate row. **Masks dropped to 3** (from 4): the header `directory:` row, the random `› <placeholder>` row, and the footer cwd — all inherently per-run. **(C) Dynamic frame:** `tui_dynamic_test.go` drives a scripted assistant turn through a fake `/v1/responses` SSE server (reusing the turn-test machinery), types a user message, polls until the reply lands + the screen is stable, captures the post-reply frame, and diffs both binaries' grids — exercising the inline history-insertion path end-to-end. The header card, idle composer, and footer match; the test is LOG-ONLY by default (own opt-in `CODEX_TUI_DYNAMIC_STRICT`, NOT the global strict flag, so idle strict-green is preserved) because it REVEALS three real codexgo history-rendering gaps for a future wave: codexgo does not echo the submitted user message (`› hi`), the agent message lacks codex's leading `• ` bullet, and the user-cell marker is `> ` vs codex's `› `. **Wave 5: `/clear` + footer theme colors (final TUI wave).** **(A) `/clear`:** the slash command is now fully wired (`internal/tui/clear.go`). `ClearUIEvent` routes through `Model.handleClearUI` (port of the `AppEvent::ClearUi` arm in `app/event_dispatch.rs`): it (1) emits the EXACT codex terminal-clear sequence `\x1b[r\x1b[0m\x1b[H\x1b[2J\x1b[3J\x1b[H` — DECSTBM reset + SGR reset + cursor-home + ED2 (visible-screen erase) + ED3 (scrollback purge) + cursor-home, captured byte-for-byte from codex 0.136.0 and matching `custom_terminal::clear_scrollback_and_visible_screen_ansi` — written raw to the program output (`Model.output`, default `os.Stdout`) ahead of `tea.ClearScreen` (renderer reset) so the purge the managed renderer omits is included; (2) resets the transcript via the new `ClearResettable` seam (`layout.go`) — `ChatTranscript.ResetForClear` discards all cells + the active stream + the scrollback-drain bookkeeping and re-seeds the session-header card from the retained header config (port of `reset_transcript_state_after_clear` + the fresh-session header insertion); (3) re-drains the fresh header into native scrollback AFTER the clear, sequenced so it lands below the cleared screen. A composer-fix was needed first: **Enter on the slash popup now DISPATCHES the highlighted command** (`composer.go` `dispatchSelectedSlash`, port of `slash_input.rs handle_key_event_with_slash_popup` → `InputResult::Command`), instead of merely completing the text — without it `/clear` could never fire from the popup. The new `tui_clear_test.go` frame scenario types `/clear`, waits for the popup, presses Enter, and compares the POST-clear fresh-idle frame: **0 row + 0 attribute mismatches at both sizes, strict level 2, stable across 3 consecutive runs.** **(B) Footer theme colors:** codex renders the default status line with per-segment softened theme colors, not a flat dim. `internal/tui/footer_color.go` ports `soften_status_line_color` (`status_line_style.rs`; verified `soften(255,0,0)==(228,11,11)`, the Rust unit-test value) and pins the resolved-and-softened default-theme accents captured byte-for-byte from codex at TrueColor: model `#f6e2b7` (246,226,183), path `#abdfa7` (171,223,167), separator dim. `ComposerFooter.styledLine` (`composer_footer.go`) builds the footer as styled spans — model span / dim ` · ` separator / path span over a 2-col unstyled indent gutter — and renders them via a TrueColor-pinned lipgloss renderer (`styled.go` `trueColorRenderer` + `Line.RenderWith`), because ratatui serializes `Color::Rgb` as 24-bit truecolor UNCONDITIONALLY (it never downsamples to a 256-indexed approximation the way `best_color` does for other surfaces), so the footer must too regardless of the terminal's reported depth. The mask is now tightened: `tui_footer_test.go` locates the footer row and compares its fixed PREFIX (indent + `<model> <reasoning>` span + ` · `) at the glyph AND per-cell attribute layer — only the trailing per-run cwd path is excluded — proving the softened-RGB model accent + dim separator are **byte-identical (0 glyph + 0 attribute mismatches, both sizes, stable across 3 runs).** **Remaining:** (1) align codexgo's user-echo + agent-bullet history cells so the dynamic frame passes strict — **CLOSED in wave 4** (the dynamic frame is now strict-green on the global ladder); (2) the startup tooltip is excluded (inherently non-deterministic); (3) resize reflow of already-scrolled history is not yet frame-tested (a `/clear` frame test landed in wave 5); (4) the footer's path-span color depends on the same per-run cwd it carries, so only the model-span color is frame-compared (the path color is pinned by unit test); the broader syntect TextMate theme resolution remains unported (the default-theme accents are hardcoded from the captured values). | wave 5 closes /clear + footer theme colors; remaining: syntect theme resolution for non-default themes + resize-reflow frame test | review |
| 44 doctor | format | **Closed for the JSON shape + key sets.** `doctor --json` now emits each check's `details` as a keyed object (label → string, or array for repeated labels), with `{}` for the empty case and a `notes` array for non-`label: value` strings — byte-identical to codex's `structured_json_details`/`JsonDoctorCheck` (`doctor_details.go`, `doctorCheck.MarshalJSON`). All 18 checks' detail key sets + value types match `/tmp/codex_doctor.json` on this machine; verified by `TestDoctorDetailKeySetsMatchCodex`. New plumbing ported faithfully: config.load feature-flag rows (enabled/overrides via `features.FromSources`) + config.toml path/parse array; runtime.provenance `commit` (ldflags `BuildCommit`, default "unknown") + descriptive install context; installation managed-package-root + `which -a codex` rows; git.environment PATH candidates + exec-path + build-options + `.git entry`; terminal.env terminal name/version/size/`color output`; terminal.title project source/value; state.paths active/archived rollout stats; updates.status `update action` + version-cache array + network-gated latest-version probe; websocket provider metadata + `auth mode` + DNS family. | **Value-fidelity gaps (key present, value best-effort):** system.environment `os`/`os type`/`os version` use sw_vers/os-release/uname (no `os_info` crate); sandbox.helpers `execve wrapper helper` is `none` (no Arg0DispatchPaths plumbing) and filesystem/network sandbox derive from `sandbox_mode` (not the full `Permissions` model); state.rollout_db_parity always takes the state-DB-missing path (no rollout/thread state DB), so `rollout DB rows` is `skipped`, malformed-name/scan-cap are `0`/`false`; websocket `handshake transport error`/`DNS`/`handshake result` come from a bounded HTTPS HEAD probe + independent DNS lookup, not codex's live Responses-WS handshake (`handshake result` vs `handshake transport error` is probe-outcome-conditional). Environment-conditional keys (reachability label by auth mode, proxy/terminal/install rows) match under the same environment. | review |

> Entries are appended as each spec lands and reports its deviations.
> **`review (must finish)`** items are genuine gaps to close before claiming the
> corresponding spec complete — they are not permanent accepted deviations.
