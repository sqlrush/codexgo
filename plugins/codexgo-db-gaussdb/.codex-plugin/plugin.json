{
  "name": "codexgo-db-gaussdb",
  "version": "0.8.0",
  "description": "GaussDB / openGauss health-check and SQL-tuning tools for codexgo, exposed as an external MCP server (read-only).",
  "keywords": ["gaussdb", "opengauss", "database", "health-check", "sql-tuning", "dba"],
  "mcpServers": "./.mcp.json",
  "interface": {
    "displayName": "GaussDB 数据库专家",
    "shortDescription": "GaussDB / openGauss 健康检查与 SQL 调优(只读)",
    "category": "database",
    "capabilities": ["mcp"],
    "brandColor": "#0a7cff"
  }
}
